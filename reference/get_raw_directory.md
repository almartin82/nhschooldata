# Get raw school directory data from NH DOE

Downloads the raw school directory data from the New Hampshire
Department of Education Profiles system. Tries live download first, then
falls back to bundled data.

## Usage

``` r
get_raw_directory()
```

## Value

Raw data frame as downloaded from NH DOE
